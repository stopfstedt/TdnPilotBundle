{% block extras_methods %}

    /**
     * @return {{ entity }}Handler
     */
    protected function get{{ entity }}Handler()
    {
        return $this->container->get('{{ bundle|replace({'Bundle': ''})|lower}}.{{ entity|lower }}.handler');
    }
{% endblock extras_methods %}
