{%- block field %}
            ->add('{{ field.fieldName }}', 'entity', [
                'required' => false,
                'class' => "{{ field.relatedEntityShortcut }}"
            ])
{% endblock -%}
